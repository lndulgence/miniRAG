apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Rageddy.fullname" . }}-test-connection"
  labels:
    {{- include "Rageddy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Rageddy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
